import React from 'react';

const About: React.FC = () => {
    return (
        <div>
            <h1>About Task Manager</h1>
            <p>This application helps you manage your tasks efficiently.</p>
            <p>Features include adding, editing, and deleting tasks, as well as viewing all tasks in a list.</p>
            <p>Built with React and TypeScript, this app utilizes the Context API for state management.</p>
        </div>
    );
};

export default About;